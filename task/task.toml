artifacts = ["/app/report.json"]

[task]
name = "dynamo/log-report"
description = "Parse an Apache-style access log into a small JSON summary report."

[metadata]
category = "data_processing_and_etl"
subcategory = "text_processing"

task_objective = "Transform an Apache-style access log into a JSON summary report."
artifact_type = "generated_output_artifact"

expert_time_estimate_hours = 0.3

model_tested = "GPT-5.4"
agent_tested = "Terminus-2"

difficulty_explanation = "Parse an Apache-style access log and generate a JSON summary containing request statistics."

solution_explanation = "Read the access log, count total requests, count unique client IP addresses, determine the most frequently requested URL path, and write the results to /app/report.json."

verification_explanation = "Verify that /app/report.json exists, contains valid JSON, and has the expected values for total_requests, unique_ips, and top_path."

[verifier]
timeout_sec = 120

[agent]
timeout_sec = 320

[environment]
build_timeout_sec = 600
cpus = 1
memory_mb = 2048
storage_mb = 10240
gpus = 0
allow_internet = false
mcp_servers = []